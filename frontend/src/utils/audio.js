/**
 * Audio feedback utility.
 * Kept silent / no-op for now as physical barcode readers have built-in beepers.
 */

export function playBeepSuccess() {
    // Silent no-op
}

export function playBeepWarning() {
    // Silent no-op
}
